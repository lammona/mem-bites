import styles from "./mem_bite.module.css";

//component to display individual mem bites -
// it will be created of the topic from the form in page.tsx /new_topic and card components aproved in the /memBite page
export default function Mem_bite() {
  return <button className={styles.mem}>mem-bite</button>;
}
